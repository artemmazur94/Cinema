using System;
using System.Collections.Generic;

namespace Cinema.DataAccess.Repositories.Contracts
{
    public interface IMovieRepository : IDisposable, IRepository<Movie>
    {
        MovieLocalization GetMovieLocalization(int id, int languageId);

        List<string> GetActorLocalizations(ICollection<Person> actors, int languageId);

        string GetDirectorLocalization(int directorId, int languageId);

        void AddMovieLocalization(MovieLocalization movieLocalization);

        void UpdateMovie(MovieLocalization movieLocalization);

        List<Person> GetSelectedActors(List<int> actorIds);

        void AddPoster(Photo photo);

        void UpdateMoviePoster(Photo photo, int movieId);

        void DeletePoster(Photo photo);

        Photo GetPosterByMovieId(int movieId);
    }
}